When working with code, follow these practices:

- Write clean, readable code with meaningful variable names
- Add type hints (Python) or type annotations (TypeScript) to function signatures
- Keep functions small and focused on a single responsibility
- Handle errors gracefully — don't swallow exceptions silently
- Use the language's idiomatic patterns and conventions
